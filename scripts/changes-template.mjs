export function renderChangesPage(snapshot) {
  const history = snapshot.wrHistory || { trackingStartedAt: snapshot.fetchedAt, days: [] };
  const data = JSON.stringify(history).replace(/</g, "\\u003c");
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>BoatLabs · Daily WR Changes</title><style>:root{color-scheme:dark;--bg:#080b10;--panel:#10161f;--line:#283242;--text:#f3f5f7;--muted:#9ca8b5;--gold:#ffc85c;--green:#64df9b;--red:#f3948c}*{box-sizing:border-box}body{margin:0;min-height:100vh;background:radial-gradient(900px 500px at 90% -20%,#1a2636,transparent 66%),var(--bg);color:var(--text);font:14px/1.45 Inter,system-ui,sans-serif}.shell{width:min(940px,calc(100% - 28px));margin:auto;padding:28px 0}.top{display:flex;align-items:center;gap:12px}.mark{width:38px;height:38px;display:grid;place-items:center;background:var(--gold);color:#15100a;font-weight:900;clip-path:polygon(0 0,100% 0,100% 72%,72% 100%,0 100%)}.eyebrow{font-size:10px;color:var(--muted);font-weight:850;letter-spacing:.13em}h1{font-size:17px;margin:2px 0 0}.back{margin-left:auto;color:var(--muted);text-decoration:none;border:1px solid var(--line);border-radius:9px;padding:8px 10px;font-size:12px;font-weight:750}.note{margin:16px 0 12px;padding:11px 13px;border:1px solid #5a4720;border-radius:11px;background:#17140e;color:#d8cca9;font-size:12px}.note b{color:var(--gold)}.days{display:flex;gap:7px;overflow:auto;padding-bottom:4px;margin-bottom:12px}.day{flex:none;border:1px solid var(--line);background:var(--panel);color:var(--muted);border-radius:8px;padding:7px 9px;font:700 11px inherit;cursor:pointer}.day.active{background:var(--gold);border-color:var(--gold);color:#15100a}.panel{border:1px solid var(--line);border-radius:13px;overflow:hidden;background:var(--panel)}.summary{padding:13px 15px;border-bottom:1px solid var(--line);color:var(--muted);font-size:12px}.summary b{color:var(--text)}.player-head{display:grid;grid-template-columns:42px minmax(0,1fr) 52px 52px 58px;gap:8px;padding:9px 15px;border-bottom:1px solid var(--line);color:var(--muted);font-size:10px;font-weight:850;letter-spacing:.08em}.player-list{border-bottom:1px solid var(--line)}.player{display:grid;grid-template-columns:42px minmax(0,1fr) 52px 52px 58px;gap:8px;align-items:center;padding:11px 15px;border-bottom:1px solid #202936}.player:last-child{border:0}.rank{color:var(--muted);font-weight:800}.player-name{font-weight:850;overflow:hidden;text-overflow:ellipsis}.stat{font-weight:850;text-align:right}.plus{color:var(--green)}.minus{color:var(--red)}.net{color:var(--text)}.event-title{padding:10px 15px 8px;color:var(--muted);font-size:10px;font-weight:850;letter-spacing:.1em}.event{padding:12px 15px;border-top:1px solid #202936}.track{color:inherit;text-decoration:none;font-weight:800}.detail{margin-top:2px;color:var(--muted);font-size:12px}.empty{padding:36px 15px;text-align:center;color:var(--muted)}@media(max-width:560px){.shell{width:min(100% - 22px,940px);padding-top:12px}.note{font-size:11px}.back{padding:7px}.player-head,.player{grid-template-columns:28px minmax(0,1fr) 42px 42px 48px;gap:5px;padding-left:11px;padding-right:11px}.player-head{font-size:9px}.event{padding:11px}.event-title{padding-left:11px}}</style></head><body><main class="shell"><header class="top"><div class="mark">BL</div><div><div class="eyebrow">COMMUNITY TOOL · UNOFFICIAL</div><h1>DAILY WR CHANGES</h1></div><a class="back" href="../">← Leaderboards</a></header><div class="note">Tracking began <b id="started"></b>. Only WR changes observed after this baseline are shown; each day covers the daily sync at <b>22:00 UTC</b> (midnight in Italy while CEST).</div><div id="days" class="days"></div><section class="panel"><div id="content"></div></section></main><script>const history=${data},days=document.getElementById('days'),content=document.getElementById('content');const fmt=d=>new Date(d).toLocaleDateString(undefined,{day:'numeric',month:'short'});document.getElementById('started').textContent=new Date(history.trackingStartedAt).toLocaleDateString(undefined,{day:'numeric',month:'short',year:'numeric'});function players(day){const all=new Set([...Object.keys(day?.gains||{}),...Object.keys(day?.losses||{})]);return [...all].map(player=>({player,gain:day.gains[player]||0,loss:day.losses[player]||0})).sort((a,b)=>(b.gain-b.loss)-(a.gain-a.loss)||b.gain-a.gain||a.loss-b.loss||a.player.localeCompare(b.player)).map((p,i)=>({...p,rank:i+1,net:p.gain-p.loss}))}function show(day){content.replaceChildren();const list=players(day),sum=document.createElement('div');sum.className='summary';sum.innerHTML=day?'<b>'+fmt(day.at)+'</b> · '+list.length+' player'+(list.length===1?'':'s')+' changed WR ownership · '+day.events.length+' tracked change'+(day.events.length===1?'':'s'):'';content.append(sum);if(!day||!day.events.length){const empty=document.createElement('div');empty.className='empty';empty.textContent='No WR holder changes were observed in this sync.';content.append(empty);return}if(list.length){const head=document.createElement('div');head.className='player-head';head.innerHTML='<span>RANK</span><span>PLAYER</span><span class="stat">WON</span><span class="stat">LOST</span><span class="stat">NET</span>';content.append(head);const wrap=document.createElement('div');wrap.className='player-list';list.forEach(p=>{const row=document.createElement('div');row.className='player';row.innerHTML='<span class="rank">#'+p.rank+'</span><span class="player-name"></span><span class="stat plus">+'+p.gain+'</span><span class="stat minus">−'+p.loss+'</span><span class="stat net">'+(p.net>0?'+':'')+p.net+'</span>';row.querySelector('.player-name').textContent=p.player;wrap.append(row)});content.append(wrap)}const title=document.createElement('div');title.className='event-title';title.textContent='TRACK-BY-TRACK HISTORY';content.append(title);day.events.forEach(e=>{const row=document.createElement('div');row.className='event';const link=document.createElement('a');link.className='track';link.href='https://boatlabs.net/tracks/'+encodeURIComponent(e.commandName);link.target='_blank';link.textContent=e.track;const d=document.createElement('div');d.className='detail';d.textContent=e.type==='take'?e.player+' took the WR from '+e.from+' · '+e.fromTime+' → '+e.time:e.type==='new'?e.player+' set the first tracked WR · '+e.time:e.player+' improved their own WR · '+e.fromTime+' → '+e.time;row.append(link,d);content.append(row)})}const ordered=[...history.days].reverse();if(!ordered.length){show(null)}else{ordered.forEach((day,i)=>{const b=document.createElement('button');b.className='day '+(!i?'active':'');b.textContent=fmt(day.at);b.onclick=()=>{document.querySelectorAll('.day').forEach(x=>x.classList.remove('active'));b.classList.add('active');show(day)};days.append(b)});show(ordered[0])}</script></body></html>`;
}
