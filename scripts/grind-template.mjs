export function renderGrindPage(snapshot) {
  const data = JSON.stringify(snapshot.grindTracks || []).replace(/</g, "\\u003c");
  const updated = new Date(snapshot.fetchedAt).toLocaleString(undefined, { dateStyle: "medium", timeStyle: "short" });
  return `<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="description" content="Most grinded BoatLabs tracks, ranked by public total time spent.">
  <title>BoatLabs · Most Grinded Tracks</title>
  <style>
    :root{color-scheme:dark;--bg:#080b10;--panel:#10151d;--line:#242c37;--text:#f3f5f7;--muted:#9ca8b5;--gold:#ffc85c;--green:#54dc96}*{box-sizing:border-box}html,body{height:100%;overflow:hidden}body{margin:0;background:radial-gradient(900px 500px at 90% -20%,#1a2636 0%,transparent 66%),var(--bg);color:var(--text);font:15px/1.45 Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}.shell{width:min(1040px,calc(100% - 32px));height:100dvh;margin:auto;padding:26px 0 16px;display:flex;flex-direction:column;min-height:0}.top{flex:none;display:flex;align-items:center;gap:13px;margin-bottom:14px}.mark{width:38px;height:38px;display:grid;place-items:center;background:var(--gold);color:#15100a;font-weight:900;letter-spacing:-1px;clip-path:polygon(0 0,100% 0,100% 72%,72% 100%,0 100%)}.eyebrow{font-size:10px;letter-spacing:.14em;color:var(--muted);font-weight:800}.top h1{font-size:16px;letter-spacing:.03em;margin:2px 0 0}.back{margin-left:auto;border:1px solid var(--line);border-radius:9px;padding:8px 10px;color:var(--muted);text-decoration:none;font-size:12px;font-weight:750}.back:hover{color:var(--text);background:#151c26}.meta{margin-left:4px;text-align:right;color:var(--muted);font-size:12px}.meta strong{display:block;color:var(--text);font-weight:650}.intro{flex:none;margin:0 0 13px;padding:12px 14px;border:1px solid #5a4720;border-radius:12px;background:linear-gradient(100deg,rgba(112,77,20,.28),rgba(22,25,30,.88))}.intro h2{margin:0;color:var(--gold);font-size:11px;letter-spacing:.1em}.intro p{margin:4px 0 0;color:#d5c8aa;font-size:12px}.range{display:flex;gap:8px;flex-wrap:wrap;margin-top:9px}.range span{border:1px solid rgba(255,200,92,.25);border-radius:6px;padding:4px 6px;background:rgba(8,11,16,.28);font-size:11px}.range b{color:var(--gold)}.tools{flex:none;margin:0 0 11px}.search{width:100%;border:1px solid var(--line);background:#0c1118;border-radius:10px;color:var(--text);padding:11px 13px;font:inherit;outline:none}.search:focus{border-color:#677586}.panel{flex:1;min-height:0;display:flex;flex-direction:column;border:1px solid var(--line);border-radius:14px;overflow:hidden;background:linear-gradient(160deg,#111720,#0d1219)}.head,.row{display:grid;grid-template-columns:66px minmax(0,1fr) 154px 132px;gap:12px;align-items:center}.head{flex:none;padding:13px 16px;border-bottom:1px solid var(--line);color:var(--muted);font-size:10px;font-weight:800;letter-spacing:.12em}.rows{min-height:0;overflow-y:auto;overscroll-behavior:contain;scrollbar-color:#3a4758 transparent}.row{padding:11px 16px;border-bottom:1px solid #202833;text-decoration:none;color:inherit}.row:hover{background:#18212c}.rank{color:var(--gold);font-weight:850}.track{font-weight:760;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.spent{color:#c4ced8;font-variant-numeric:tabular-nums}.mult{justify-self:start;min-width:74px;text-align:center;border:1px solid #3a4758;background:#1a2532;border-radius:6px;padding:5px 7px;font-weight:900;font-variant-numeric:tabular-nums}.mult.high{border-color:#8a6924;background:#3f3013;color:#ffe4a3}.mult.low{border-color:#596471;background:#202934;color:#c5cfda}.tag{margin-left:6px;padding:3px 5px;border-radius:4px;background:rgba(255,200,92,.16);color:var(--gold);font-size:9px;font-weight:900;letter-spacing:.05em}.tag.low{background:rgba(169,185,200,.13);color:#c3cfda}.empty{padding:28px;text-align:center;color:var(--muted)}.foot{flex:none;margin-top:12px;color:#748190;font-size:12px}.foot a{color:#b9c6d3;text-decoration:none}@media(max-width:620px){.shell{width:min(100% - 22px,1040px);padding-top:10px;padding-bottom:10px}.top{gap:8px;margin-bottom:8px}.mark{width:32px;height:32px;font-size:12px}.eyebrow{display:none}.top h1{font-size:14px;margin:0}.meta{display:none}.back{padding:7px 8px;font-size:11px}.intro{margin-bottom:8px;padding:9px}.intro p{font-size:10px;line-height:1.3}.range{gap:5px;margin-top:6px}.range span{font-size:10px;padding:3px 5px}.tools{margin-bottom:8px}.search{padding:9px 11px;font-size:14px}.head,.row{grid-template-columns:38px minmax(0,1fr) 78px;gap:7px;padding-left:11px;padding-right:11px}.head span:nth-child(3),.row .spent{display:none}.head{padding-top:10px;padding-bottom:10px}.mult{min-width:0;width:78px;font-size:11px}.tag{display:none}}
  </style>
</head>
<body>
  <main class="shell">
    <header class="top"><div class="mark">BL</div><div><div class="eyebrow">COMMUNITY TOOL · UNOFFICIAL</div><h1>MOST GRINDED TRACKS</h1></div><a class="back" href="../">← Leaderboards</a><div class="meta"><strong>Snapshot · ${updated}</strong><span>${snapshot.tracksScanned} public tracks</span></div></header>
    <section class="intro"><h2>TOTAL TIME SPENT · GRIND MULTIPLIER</h2><p>Eligible tracks are ranked only by public total time spent. The multiplier shown is the one currently used for Performance points.</p><div class="range"><span>Top 4%: <b>2.10×</b></span><span>Bottom 4%: <b>0.476×</b></span><span>New tracks: <b>1.00×</b> until day 30</span><span>Days 31–60: <b>smooth fade-in</b></span></div></section>
    <div class="tools"><input id="search" class="search" type="search" autocomplete="off" placeholder="Search a track"></div>
    <section class="panel"><div class="head"><span>RANK</span><span>TRACK</span><span>TOTAL TIME</span><span>MULTIPLIER</span></div><div id="rows" class="rows"></div></section>
    <footer class="foot">Only tracks with a completed time and at least 30 days of age are ranked. Data source: <a href="https://boatlabs.net/tracks" target="_blank" rel="noreferrer">BoatLabs tracks</a>.</footer>
  </main>
  <script>
    const tracks=${data}; const rows=document.getElementById("rows"); const search=document.getElementById("search");
    function formatDuration(milliseconds){let seconds=Math.floor(Number(milliseconds)/1000);const days=Math.floor(seconds/86400);seconds%=86400;const hours=Math.floor(seconds/3600);seconds%=3600;const minutes=Math.floor(seconds/60);return days?days+"d "+hours+"h":hours?hours+"h "+minutes+"m":minutes+"m"}
    function render(){const query=search.value.trim().toLowerCase();rows.replaceChildren();const filtered=tracks.filter(track=>!query||track.track.toLowerCase().includes(query));if(!filtered.length){rows.innerHTML='<div class="empty">No track matches that search.</div>';return}filtered.forEach((track,index)=>{const row=document.createElement("a");row.className="row";row.href="https://boatlabs.net/tracks/"+encodeURIComponent(track.commandName);row.target="_blank";row.rel="noreferrer";const rank=document.createElement("span");rank.className="rank";rank.textContent="#"+(index+1);row.append(rank);const name=document.createElement("span");name.className="track";name.textContent=track.track;row.append(name);const spent=document.createElement("span");spent.className="spent";spent.textContent=formatDuration(track.totalTimeSpent);row.append(spent);const mult=document.createElement("span");const top=track.percentile>=.96,low=track.percentile<=.04;mult.className="mult "+(top?"high":low?"low":"");mult.textContent="×"+track.multiplier.toFixed(3);if(top||low){const tag=document.createElement("span");tag.className="tag "+(low?"low":"");tag.textContent=top?"TOP 4%":"BOTTOM 4%";mult.append(tag)}row.append(mult);rows.append(row)})}search.oninput=render;render();
  </script>
</body>
</html>`;
}
