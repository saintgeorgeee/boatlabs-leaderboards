export function renderPerformancePage(snapshot) {
  const data = JSON.stringify(snapshot).replace(/</g, "\\u003c");
  return `<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="description" content="Unofficial daily BoatLabs performance leaderboard.">
  <title>BoatLabs Performance Leaderboard</title>
  <style>
    :root{color-scheme:dark;--bg:#080b10;--panel:#10151d;--line:#242c37;--text:#f3f5f7;--muted:#9ca8b5;--gold:#ffc85c;--green:#54dc96}*{box-sizing:border-box}html,body{height:100%;overflow:hidden}body{margin:0;background:radial-gradient(900px 500px at 90% -20%,#1a2636 0%,transparent 66%),var(--bg);color:var(--text);font:15px/1.45 Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}a{color:inherit}.shell{width:min(1180px,calc(100% - 32px));height:100dvh;margin:auto;padding:26px 0 16px;display:flex;flex-direction:column;min-height:0}.top{flex:none;display:flex;align-items:center;gap:13px;margin-bottom:16px}.mark{width:38px;height:38px;display:grid;place-items:center;background:var(--gold);color:#15100a;font-weight:900;letter-spacing:-1px;clip-path:polygon(0 0,100% 0,100% 72%,72% 100%,0 100%)}.eyebrow{font-size:10px;letter-spacing:.14em;color:var(--muted);font-weight:800}.top h1{font-size:16px;letter-spacing:.03em;margin:2px 0 0}.nav{margin-left:auto;border:1px solid var(--line);border-radius:8px;padding:8px 10px;color:var(--muted);font-size:12px;font-weight:750;text-decoration:none}.nav:hover{color:var(--text);border-color:#566476;background:#151c26}.meta{text-align:right;color:var(--muted);font-size:12px}.meta strong{display:block;color:var(--text);font-weight:650}.tools{flex:none;display:flex;gap:10px;align-items:center;margin:0 0 14px}.search{flex:1;min-width:0;border:1px solid var(--line);background:#0c1118;border-radius:10px;color:var(--text);padding:12px 14px;font:inherit;outline:none}.search:focus{border-color:#677586}.toggle{white-space:nowrap;display:flex;gap:8px;align-items:center;color:var(--muted);font-size:13px}.toggle input{accent-color:var(--gold);width:16px;height:16px}.layout{flex:1;min-height:0;display:grid;grid-template-columns:minmax(0,1.2fr) minmax(310px,.8fr);gap:16px}.panel{min-height:0;display:flex;flex-direction:column;border:1px solid var(--line);background:linear-gradient(160deg,#111720,#0d1219);border-radius:14px;overflow:hidden}.list-head{flex:none;padding:14px 16px;border-bottom:1px solid var(--line);display:grid;grid-template-columns:52px 1fr 76px 64px;gap:8px;color:var(--muted);font-size:10px;font-weight:800;letter-spacing:.12em}#rows,.cards{min-height:0;overflow-y:auto;overscroll-behavior:contain;scrollbar-color:#3a4758 transparent}#rows{flex:1}.row{width:100%;border:0;border-bottom:1px solid #202833;background:transparent;color:inherit;padding:10px 16px;display:grid;grid-template-columns:52px 1fr 76px 64px;gap:8px;align-items:center;text-align:left;font:inherit;cursor:pointer}.row.podium-gold{background:linear-gradient(105deg,rgba(255,200,92,.30),rgba(112,77,20,.31) 54%,rgba(255,226,143,.12));border-bottom-color:#856321}.row.podium-silver{background:linear-gradient(105deg,rgba(216,226,235,.27),rgba(85,102,118,.31) 54%,rgba(231,239,246,.10));border-bottom-color:#697887}.row.podium-bronze{background:linear-gradient(105deg,rgba(218,143,91,.29),rgba(111,60,31,.31) 54%,rgba(244,177,124,.10));border-bottom-color:#814c2b}.row:hover,.row:focus-visible{background:#19212c;outline:0}.rank{color:var(--gold);font-weight:800}.who{display:flex;min-width:0;align-items:center;gap:10px}.avatar{width:30px;height:30px;flex:0 0 30px;border-radius:7px;overflow:hidden;background:#263140;display:grid;place-items:center;color:#dce8f7;font-size:10px;font-weight:800}.avatar img{width:100%;height:100%;object-fit:cover}.name{font-weight:750;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.score{font-size:16px;font-weight:850}.placements{color:var(--text);opacity:.68;font-size:12px;font-weight:500}.empty{padding:28px 16px;color:var(--muted);text-align:center}.detail-empty{padding:28px;color:var(--muted)}.detail-head{flex:none;padding:18px;border-bottom:1px solid var(--line);display:flex;align-items:center;gap:11px}.detail-head .avatar{width:42px;height:42px;flex-basis:42px;border-radius:9px}.detail-title{min-width:0}.detail-title h3{font-size:18px;margin:0;overflow:hidden;text-overflow:ellipsis}.detail-title p{margin:1px 0 0;color:var(--muted);font-size:12px}.close{margin-left:auto;border:1px solid var(--line);background:transparent;color:var(--muted);width:31px;height:31px;border-radius:8px;font-size:20px;cursor:pointer}.close:hover{color:var(--text);background:#202a36}.cards{flex:1;padding:12px;display:grid;grid-template-columns:repeat(2,minmax(0,1fr));align-content:start;gap:9px}.card{border:1px solid #26303d;background:#0a0f15;padding:11px;border-radius:10px;min-width:0}.track-link{display:block;text-decoration:none;font-weight:760;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.track-link:hover{color:var(--gold)}.card-bottom{margin-top:9px;display:flex;align-items:center;gap:7px}.place{font-size:10px;line-height:1;padding:5px 6px;border-radius:4px;font-weight:900;letter-spacing:.06em;background:#273342;color:#d8e6f4}.badge{font-size:9px;line-height:1;padding:5px 6px;border-radius:4px;font-weight:900;letter-spacing:.06em;background:#273342;color:#d8e6f4}.badge.beginner{background:#163c2a;color:#6cf0a7}.badge.easy{background:#123b42;color:#70ecf3}.badge.medium{background:#4a3714;color:#ffd36e}.badge.hard{background:#4b2320;color:#ffaaa3}.badge.expert{background:#38214d;color:#d7adff}.time{margin-left:auto;font-variant-numeric:tabular-nums;font-weight:780}.check{color:var(--green);font-size:16px;font-weight:900;line-height:1}.foot{flex:none;margin-top:12px;color:#748190;font-size:12px}.foot a{color:#b9c6d3;text-decoration:none}.foot a:hover{text-decoration:underline}@media(max-width:820px){.layout{grid-template-columns:1fr;grid-template-rows:minmax(0,1fr) minmax(0,1fr)}.meta{display:none}}@media(max-width:620px){.shell{width:min(100% - 22px,1180px);padding-top:16px}.top{margin-bottom:12px}.nav{margin-left:auto}.tools{align-items:stretch;flex-wrap:wrap}.search{flex-basis:100%}.toggle{margin-left:2px}.list-head,.row{grid-template-columns:42px 1fr 62px 54px;padding-left:11px;padding-right:11px}.cards{grid-template-columns:1fr}.foot{display:none}}
  </style>
</head>
<body>
  <main class="shell">
    <header class="top">
      <div class="mark">BL</div>
      <div><div class="eyebrow">COMMUNITY TOOL · UNOFFICIAL</div><h1>BOATLABS PERFORMANCE LEADERBOARD</h1></div>
      <a class="nav" href="../">WR leaderboard</a>
      <div class="meta"><strong id="updated">Loading snapshot…</strong><span id="coverage"></span></div>
    </header>
    <div class="tools"><input id="search" class="search" type="search" autocomplete="off" placeholder="Search player or track"><label class="toggle"><input id="shared" type="checkbox" checked> Shared rank</label></div>
    <section class="layout"><div class="panel"><div class="list-head"><span>RANK</span><span>PLAYER</span><span>POINTS</span><span>TOP 20</span></div><div id="rows"></div></div><aside id="detail" class="panel detail"><div class="detail-empty">Select a player to see their top-20 placements.</div></aside></section>
    <footer class="foot">Points: P1 100 · P2 75 · P3 50 · P4 38 · P5 27 · P10 15 · P20 2 · Data source: <a href="https://boatlabs.net/tracks" target="_blank" rel="noreferrer">BoatLabs tracks</a></footer>
  </main>
  <script>
    const snapshot = ${data};
    const byId = function(id){ return document.getElementById(id); };
    const initial = function(name){ return name.split(/[_ -]/).filter(Boolean).slice(0,2).map(function(x){return x[0];}).join("").toUpperCase() || "?"; };
    function avatar(name, size){ const box=document.createElement("span");box.className="avatar "+(size||"small");const img=new Image();img.alt="";img.loading="lazy";img.src="https://mc-heads.net/avatar/"+encodeURIComponent(name)+"/48";img.onerror=function(){img.remove();box.textContent=initial(name);};box.append(img);return box; }
    function difficulty(value){ const s=String(value||"Unknown");const b=document.createElement("span");b.className="badge "+s.toLowerCase().replace(/[^a-z]/g,"");b.textContent=s.toUpperCase();return b; }
    const players=[...snapshot.performances.reduce(function(map,entry){const player=map.get(entry.player)||{name:entry.player,records:[]};player.records.push(entry);map.set(entry.player,player);return map;},new Map()).values()].map(function(player){return {...player,score:player.records.reduce(function(sum,record){return sum+record.points;},0),wins:player.records.filter(function(record){return record.position===1;}).length};}).sort(function(a,b){return b.score-a.score||b.wins-a.wins||a.name.localeCompare(b.name);});
    function rankFor(player,index){return byId("shared").checked?players.filter(function(candidate){return candidate.score>=player.score;}).length:index+1;}
    function emptyDetail(){byId("detail").innerHTML='<div class="detail-empty">Select a player to see their top-20 placements.</div>';}
    function showPlayer(player){const detail=byId("detail");detail.replaceChildren();const head=document.createElement("div");head.className="detail-head";head.append(avatar(player.name,"large"));const title=document.createElement("div");title.className="detail-title";title.innerHTML="<h3></h3><p></p>";title.querySelector("h3").textContent=player.name;title.querySelector("p").textContent=player.score+" points · "+player.records.length+" top-20 placement"+(player.records.length===1?"":"s")+" · "+player.wins+" win"+(player.wins===1?"":"s");head.append(title);const close=document.createElement("button");close.className="close";close.type="button";close.setAttribute("aria-label","Close player details");close.textContent="×";close.addEventListener("click",emptyDetail);head.append(close);detail.append(head);const cards=document.createElement("div");cards.className="cards";[...player.records].sort(function(a,b){return a.position-b.position||a.track.localeCompare(b.track);}).forEach(function(record){const card=document.createElement("article");card.className="card";const link=document.createElement("a");link.className="track-link";link.href="https://boatlabs.net/tracks/"+encodeURIComponent(record.commandName);link.target="_blank";link.rel="noreferrer";link.textContent=record.track;card.append(link);const bottom=document.createElement("div");bottom.className="card-bottom";const place=document.createElement("span");place.className="place";place.textContent="P"+record.position+" · +"+record.points;bottom.append(place,difficulty(record.difficulty));const time=document.createElement("span");time.className="time";time.textContent=record.time;bottom.append(time);if(record.verified){const check=document.createElement("span");check.className="check";check.title="Verified";check.textContent="✓";bottom.append(check);}card.append(bottom);cards.append(card);});detail.append(cards);}
    function render(){const query=byId("search").value.trim().toLowerCase();const rows=byId("rows");rows.replaceChildren();const filtered=players.filter(function(player){return !query||player.name.toLowerCase().includes(query)||player.records.some(function(record){return record.track.toLowerCase().includes(query);});});if(!filtered.length){rows.innerHTML='<div class="empty">No player or track matches that search.</div>';return;}filtered.forEach(function(player){const index=players.indexOf(player);const row=document.createElement("button");row.className="row "+(index===0?"podium-gold":index===1?"podium-silver":index===2?"podium-bronze":"");row.type="button";const rank=document.createElement("span");rank.className="rank";rank.textContent="#"+rankFor(player,index);row.append(rank);const who=document.createElement("span");who.className="who";who.append(avatar(player.name));const name=document.createElement("span");name.className="name";name.textContent=player.name;who.append(name);row.append(who);const score=document.createElement("span");score.className="score";score.textContent=player.score;row.append(score);const placements=document.createElement("span");placements.className="placements";placements.textContent=player.records.length;row.append(placements);row.addEventListener("click",function(){showPlayer(player);});rows.append(row);});}
    byId("updated").textContent="Snapshot · "+new Date(snapshot.fetchedAt).toLocaleString(undefined,{dateStyle:"medium",timeStyle:"short"});
    byId("coverage").textContent=snapshot.placements+" placements from "+snapshot.tracksScanned+" tracks"+(snapshot.failed?" · "+snapshot.failed+" unavailable":"");
    byId("search").addEventListener("input",render);byId("shared").addEventListener("change",render);render();
  </script>
</body>
</html>`;
}
