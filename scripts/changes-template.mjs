export function renderChangesPage(snapshot) {
  const history = snapshot.wrHistory || { trackingStartedAt: snapshot.fetchedAt, days: [] };
  const data = JSON.stringify(history).replace(/</g, "\\u003c");
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>BoatLabs · Daily WR Changes</title><style>:root{color-scheme:dark;--bg:#080b10;--line:#283242;--text:#f3f5f7;--muted:#9ca8b5;--gold:#ffc85c;--green:#64df9b;--red:#f3948c}*{box-sizing:border-box}body{margin:0;min-height:100vh;background:radial-gradient(900px 500px at 90% -20%,#1a2636,transparent 66%),var(--bg);color:var(--text);font:14px/1.45 Inter,system-ui,sans-serif}.shell{width:min(940px,calc(100% - 28px));margin:auto;padding:28px 0}.top{display:flex;align-items:center;gap:12px}.mark{width:38px;height:38px;display:grid;place-items:center;background:var(--gold);color:#15100a;font-weight:900;clip-path:polygon(0 0,100% 0,100% 72%,72% 100%,0 100%)}.eyebrow{font-size:10px;color:var(--muted);font-weight:850;letter-spacing:.13em}h1{font-size:17px;margin:2px 0 0}.back{margin-left:auto;color:var(--muted);text-decoration:none;border:1px solid var(--line);border-radius:9px;padding:8px 10px;font-size:12px;font-weight:750}.note{margin:16px 0 12px;padding:11px 13px;border:1px solid #5a4720;border-radius:11px;background:#17140e;color:#d8cca9;font-size:12px}.note b{color:var(--gold)}.days{display:flex;gap:7px;overflow:auto;padding-bottom:4px;margin-bottom:12px}.day{flex:none;border:1px solid var(--line);background:#10161f;color:var(--muted);border-radius:8px;padding:7px 9px;font:700 11px inherit;cursor:pointer}.day.active{background:var(--gold);border-color:var(--gold);color:#15100a}.panel{border:1px solid var(--line);border-radius:13px;overflow:hidden;background:#10161f}.summary{padding:13px 15px;border-bottom:1px solid var(--line);color:var(--muted);font-size:12px}.summary b{color:var(--text)}.event{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:12px;padding:12px 15px;border-bottom:1px solid #202936}.event:last-child{border:0}.track{color:inherit;text-decoration:none;font-weight:800}.detail{margin-top:2px;color:var(--muted);font-size:12px}.gain{color:var(--green);font-weight:800}.loss{color:var(--red);font-weight:800}.empty{padding:36px 15px;text-align:center;color:var(--muted)}@media(max-width:560px){.shell{width:min(100% - 22px,940px);padding-top:12px}.event{padding:11px}.note{font-size:11px}.back{padding:7px}}</style></head><body><main class="shell"><header class="top"><div class="mark">BL</div><div><div class="eyebrow">COMMUNITY TOOL · UNOFFICIAL</div><h1>DAILY WR CHANGES</h1></div><a class="back" href="../">← Leaderboards</a></header><div class="note">Tracking began <b id="started"></b>. Only WR changes observed after this baseline are shown; each day covers the daily sync at <b>22:00 UTC</b> (midnight in Italy while CEST).</div><div id="days" class="days"></div><section class="panel"><div id="content"></div></section></main><script>const history=${data},days=document.getElementById('days'),content=document.getElementById('content');const fmt=d=>new Date(d).toLocaleDateString(undefined,{day:'numeric',month:'short'});document.getElementById('started').textContent=new Date(history.trackingStartedAt).toLocaleDateString(undefined,{day:'numeric',month:'short',year:'numeric'});function show(day){content.replaceChildren();const entries=Object.entries(day?.gains||{}).map(([p,n])=>[p,n,day.losses[p]||0]).concat(Object.entries(day?.losses||{}).filter(([p])=>!day.gains[p]).map(([p,n])=>[p,0,n]));const sum=document.createElement('div');sum.className='summary';sum.innerHTML=day?'<b>'+fmt(day.at)+'</b> · '+entries.length+' player'+(entries.length===1?'':'s')+' involved · '+day.events.length+' tracked change'+(day.events.length===1?'':'s'):'';content.append(sum);if(!day||!day.events.length){const empty=document.createElement('div');empty.className='empty';empty.textContent='No WR holder changes were observed in this sync.';content.append(empty);return}day.events.forEach(e=>{const row=document.createElement('div');row.className='event';const left=document.createElement('div');const link=document.createElement('a');link.className='track';link.href='https://boatlabs.net/tracks/'+encodeURIComponent(e.commandName);link.target='_blank';link.textContent=e.track;const d=document.createElement('div');d.className='detail';d.textContent=e.type==='take'?e.player+' took the WR from '+e.from+' · '+e.fromTime+' → '+e.time:e.type==='new'?e.player+' set the first tracked WR · '+e.time:e.player+' improved their own WR · '+e.fromTime+' → '+e.time;left.append(link,d);const right=document.createElement('span');right.className=e.type==='take'||e.type==='new'?'gain':'';right.textContent=e.type==='take'||e.type==='new'?'+1 gained':'improved';row.append(left,right);content.append(row)})}const ordered=[...history.days].reverse();if(!ordered.length){show(null)}else{ordered.forEach((day,i)=>{const b=document.createElement('button');b.className='day '+(!i?'active':'');b.textContent=fmt(day.at);b.onclick=()=>{document.querySelectorAll('.day').forEach(x=>x.classList.remove('active'));b.classList.add('active');show(day)};days.append(b)});show(ordered[0])}</script></body></html>`;
}
